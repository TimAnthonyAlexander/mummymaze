{
  "name": "maze-escape-sprite-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Standalone tool: procedurally generate baked mummy sprite sheets for Maze Escape from a parametric three.js model. No coupling to the game.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "bake": "node scripts/bake.mjs",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "~5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
